version: "1"
spec:
  secrets:
    secrets/sample:
      properties:
        username:
          type: string
          default: some-value
          help: |
            This is the username you would normally use to connect to the
            target server.
        password:
          type: string
          input: hidden
          default: "123"
          help: |
            Be creative here!


